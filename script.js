let cart=[];let total=0;function addToCart(name,price){cart.push({name,price});total+=price;updateCart();}function updateCart(){const cartList=document.getElementById('cart-items');cartList.innerHTML='';cart.forEach(item=>{const li=document.createElement('li');li.textContent=`${item.name} - ₹${item.price}`;cartList.appendChild(li);});document.getElementById('total').textContent=total;}function placeOrder(){if(cart.length===0){alert('Your cart is empty!');return;}let orderDetails=cart.map(item=>`${item.name} ₹${item.price}`).join(', ');let whatsappURL=`https://wa.me/918103053951?text=Order:%20${encodeURIComponent(orderDetails)}%20Total:%20₹${total}`;alert('Redirecting to WhatsApp to place your order');window.open(whatsappURL,'_blank');}function searchMenu(){let input=document.getElementById('search').value.toLowerCase();let items=document.querySelectorAll('.item');items.forEach(item=>{let name=item.querySelector('h3').textContent.toLowerCase();item.style.display=name.includes(input)?'block':'none';});}